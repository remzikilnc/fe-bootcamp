import React from 'react';

const ExampleComponent = () => {
    return (
        <div>
           This is an example component from the component library
        </div>
    );
};

export default ExampleComponent;